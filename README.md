# CodeAlpha_Hangman-Game
A text-based console implementation of the classic Hangman game built using Python. The application utilizes the random module to select a word from a predefined list, handles user inputs dynamically, tracks remaining attempts, and updates the hidden word display in real-time
